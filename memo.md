# メモ

最新版のPIOだとなぜかVSCodeのTest Explorer UIでテストの出力が表示されない場合があるので，その時は以下のコマンドでテストを実行する．

```sh
pio test -vv
```

また，テストを実行する時は，C3-superminiだと面倒なので，esp32devを使う．
C3のほうは書き込みモードにするために，毎回ボードのBOOTボタンを押しながらリセットボタンを押す必要があり面倒．
